package system.routes

/** Imported into the routes compiler so that we don't have to fully qualify everything */
object Types {

  type UUID = java.util.UUID
  type UniversityID = warwick.sso.UniversityID
  type Usercode = warwick.sso.Usercode

}
